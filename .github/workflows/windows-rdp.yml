name: EnigMano Instance Deployment

on:
  workflow_dispatch:

jobs:
  deploy-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup User and Enable RDP
      shell: pwsh
      run: |
        $Password = ConvertTo-SecureString "P@ssw0rd123!" -AsPlainText -Force
        New-LocalUser -Name "admin" -Password $Password -FullName "Admin" -Description "Admin User"
        Add-LocalGroupMember -Group "Administrators" -Member "admin"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
        Write-Host "User created and RDP enabled successfully."

    - name: Download Bore and Start TCP Tunnel
      shell: pwsh
      run: |
        Write-Host "Downloading Bore..."
        
        # This is the EXACT official release link for Bore v0.5.0
        Invoke-WebRequest -Uri "https://github.com/ekzhang/bore/releases/download/v0.5.0/bore-x86_64-pc-windows-msvc.zip" -OutFile "bore.zip"
        Expand-Archive -Path "bore.zip" -DestinationPath "."
        
        Write-Host "Bore downloaded. Starting TCP tunnel..."
        
        # Start the tunnel (Map external random port to local 3389)
        $process = Start-Process -FilePath ".\bore.exe" -ArgumentList "local 3389 --to bore.pub" -RedirectStandardOutput "bore_out.txt" -RedirectStandardError "bore_err.txt" -PassThru -WindowStyle Hidden
        
        # Wait for it to connect
        Start-Sleep -Seconds 20
        
        Write-Host "================ BORE OUTPUT ================"
        Get-Content "bore_out.txt"
        Get-Content "bore_err.txt"
        Write-Host "=============================================="
        
        Write-Host "USERNAME: admin"
        Write-Host "PASSWORD: P@ssw0rd123!"
